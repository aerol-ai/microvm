name: Tests

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      docs: ${{ steps.filter.outputs.docs }}
      go_main: ${{ steps.filter.outputs.go_main }}
      go_sdk: ${{ steps.filter.outputs.go_sdk }}
      java_sdk: ${{ steps.filter.outputs.java_sdk }}
      python_sdk: ${{ steps.filter.outputs.python_sdk }}
      typescript_sdk: ${{ steps.filter.outputs.typescript_sdk }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Detect changed paths
        id: filter
        uses: dorny/paths-filter@v4
        with:
          filters: |
            docs:
              - 'docs/**'
            go_main:
              - 'cmd/**'
              - 'internal/**'
              - 'pkg/**'
              - 'go.mod'
              - 'go.sum'
            go_sdk:
              - 'sdk/go/**'
              - 'go.mod'
              - 'go.sum'
            java_sdk:
              - 'sdk/java/**'
            python_sdk:
              - 'sdk/python/**'
            typescript_sdk:
              - 'sdk/typescript/**'

  go-tests:
    needs: changes
    if: needs.changes.outputs.docs != 'true' && needs.changes.outputs.go_main == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential

      - name: Run unit tests with coverage
        run: |
          go test -count=1 -coverprofile=coverage.out ./cmd/... ./internal/... ./pkg/...
          go tool cover -func=coverage.out | tail -n 1

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v7
        with:
          name: go-coverage
          path: coverage.out

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: coverage.out
          flags: go-main
          fail_ci_if_error: false

  go-sdk-tests:
    needs: changes
    if: needs.changes.outputs.docs != 'true' && needs.changes.outputs.go_sdk == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Run Go SDK tests
        run: go test -count=1 ./sdk/go/...

  java-sdk-tests:
    needs: changes
    if: needs.changes.outputs.docs != 'true' && needs.changes.outputs.java_sdk == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sdk/java
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
          cache-dependency-path: sdk/java/pom.xml

      - name: Run Java SDK tests
        run: mvn -B -ntp test

  python-sdk-tests:
    needs: changes
    if: needs.changes.outputs.docs != 'true' && needs.changes.outputs.python_sdk == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sdk/python
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: sdk/python/pyproject.toml

      - name: Install Python SDK dependencies
        run: python -m pip install .

      - name: Run Python SDK tests
        run: python -m unittest discover -s tests -v

  typescript-sdk-tests:
    needs: changes
    if: needs.changes.outputs.docs != 'true' && needs.changes.outputs.typescript_sdk == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sdk/typescript
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: sdk/typescript/package-lock.json

      - name: Install TypeScript SDK dependencies
        run: npm ci

      - name: Build TypeScript SDK
        run: npm run build

      - name: Run TypeScript SDK tests
        run: npm test