---
title: Cluster Secrets Providers
description: Choose local or AWS KMS for sealing cluster credentials, understand the D5 recipient-set limitation and peer-auth trust model, and configure fan-out for HA failover.
---

Registry passwords and mount credentials for `failover.policy=recreate` sandboxes are sealed outside the Raft log. Enterprise creates wait for a bounded first backup acknowledgement; remaining replicas are distributed asynchronously. This page covers which secret provider to pick, what still requires fan-out, and the recipient-set membership limitation.

For the broader failover model see [Durability & Failover](/durability). For cluster env vars see [Cluster Glossary](/cluster-glossary).

## Two walls, two fixes

Cross-node recreate historically failed for two independent reasons:

| Wall | Symptom | Fix |
|---|---|---|
| **WALL 1** — bytes never left the sealing node | `cluster secret ref ... not found` on the new owner | Bounded first-ACK replication followed by async peer fan-out of the sealed `cluster_secrets` row (`SB_SECRET_RECIPIENT_FANOUT_ENABLED`, default on) |
| **WALL 2** — envelope recipient binding | `recipient ... is not allowed to open` even with the bytes | Local provider: seal to owner + N backup candidates. **Or** use AWS KMS so IAM, not the recipient list, authorizes unwrap |

A shared credential encryption key (`SB_CREDENTIAL_ENCRYPTION_KEY`) is necessary for the local provider but **not sufficient** for HA. Fan-out (or KMS + fan-out) is also required.

## Provider choice

`SB_SECRET_PROVIDER` defaults to `local`. A deployment that never sets it never contacts AWS, Vault, or any external service.

| Value | Key custody | Who can Open | When to use |
|---|---|---|---|
| `local` (default) | Node-local AES key file / `SB_CREDENTIAL_ENCRYPTION_KEY` | Only nodes in the seal recipient set | Default clusters; no external dependency |
| `awskms` | AWS KMS wraps the per-secret data key into the existing envelope `WrappedKey` field (no format change) | Any node with IAM Decrypt **that also received the sealed row** | HA that must survive membership change after create (D5 escape hatch) |
| `vault` | Planned — same `Provider` interface | — | Not implemented; setting this value fails boot with a clear error (no silent fallback to local) |

Both providers still store ciphertext in the local `cluster_secrets` SQLite table. KMS wraps *keys*; it does not store your payload. Fan-out machinery is shared.

### Environment variables

| Variable | Default | Description |
|---|---|---|
| `SB_ENTERPRISE_MODE` | `false` | Enables fail-closed production invariants. Cluster mode then requires mTLS, secure gossip/credentials, fan-out to at least two backups, and a non-zero first-ACK wait. Single-node and local development remain unchanged unless this is enabled. |
| `SB_SECRET_PROVIDER` | `local` | `local` \| `awskms` \| `vault` |
| `SB_SECRET_AWS_KMS_KEY_ID` | (empty) | Required when provider is `awskms` — CMK id, ARN, or alias |
| `SB_SECRET_PROVIDER_STRICT_BOOT` | `false` | When `true`, daemon start fails if the awskms wrap/unwrap canary fails. Required for `awskms` in enterprise mode |
| `SB_SECRET_RECIPIENT_FANOUT_ENABLED` | `true` | Recipient-set sealing + async peer fan-out for recreate sandboxes |
| `SB_SECRET_RECIPIENT_BACKUP_COUNT` | `2` | Non-owner candidates in the seal set (owner + this many) |
| `SB_SECRET_FANOUT_MIN_ACK_WAIT` | `2s` | Bounded sync wait for ≥1 peer ACK on HA create; `0` is allowed only outside enterprise mode |
| `SB_CREDENTIAL_ENCRYPTION_KEY` | (empty) | Shared AES key for the local provider (cluster mode requires this or a key file) |
| `SB_SECRET_ENV_SEAL_ENABLED` | `false` | Sealed sandbox env rows (T7). Format-change flag — enable only after every node can merge env from the provider bag |
| `SB_SECRET_TOOLBOX_SEAL_ENABLED` | `false` | Encrypt per-sandbox toolbox bearer tokens in the existing sandbox row. Required in enterprise mode |
| `SB_SECRET_AUDIT_STRICT_BOOT` | `true` | Refuse startup when the local audit writer cannot be opened. Required in enterprise mode |

## Known limitation (D5): frozen recipient sets

Recipient sets are computed at create (from the placement candidate list) and **do not update** when membership changes later.

With the **local** provider:

1. Create an HA sandbox sealed to `{node-a, node-b, node-c}`.
2. Add `node-d`, drain or lose the original recipients.
3. Failover may pick `node-d`, which has neither the bytes nor recipient permission.
4. Open fails with a distinct `recipient denied` error (not a generic decrypt failure). The owner watcher must not reassign-churn the fleet on that class of error.

**Documented, not fixed** for the local provider. Operators who need HA that survives membership churn should set `SB_SECRET_PROVIDER=awskms`. KMS removes WALL 2 (recipient binding) for nodes whose IAM can Decrypt — but those nodes still need the sealed row via fan-out (WALL 1).

## Peer authentication and authorization

When `SB_CLUSTER_TLS_DIR` is configured, cluster-internal requests use the
dedicated HTTPS listener and require a client certificate signed by the cluster
CA. Enterprise cluster mode requires this configuration and rejects internal
routes on the public listener; a failed mTLS request is not retried over the
public PAT-only path. The fleet PAT remains a second operator authorization
check. Outside enterprise mode, clusters without TLS retain the legacy public
PAT-only path for rolling and local compatibility; it is not the production
trust model.

| Caller | Outcome |
|---|---|
| No credential | Rejected (401) |
| Managed **tenant** token | Rejected on secret PUT/DELETE and peer-local audit (403) — operator-only |
| Valid cluster client certificate + fleet **PAT** (`SB_PAT_TOKEN`) | Allowed on the internal listener |

Receivers also check that the sealed blob’s `ref` matches `sandbox_id`/`version`
and that **this node is listed as a recipient** before storing ciphertext. A
peer PUT must also match a live placement and its exact current recipient set,
which prevents a delayed message from resurrecting a deleted secret.

The certificate proves membership in the cluster CA trust domain, not a
SPIFFE-style node identity bound to a particular Raft node ID. Protect the CA
private key and fleet PAT as cluster-root credentials, rotate both after a
suspected compromise, and keep the internal listener on a private network.

## First-ACK replication and `failover_ready`

Fan-out may wait briefly for a first peer ACK (`SB_SECRET_FANOUT_MIN_ACK_WAIT`,
default 2s), then continues asynchronously.

- `failover_ready` starts false and becomes true only when the owner plus at least one backup hold the sealed row.
- In enterprise mode, zero backup ACKs retract the new secret, enqueue deletion cleanup, and fail the create. A successful create therefore has at least one acknowledged backup.
- Outside enterprise mode, a zero-ACK create may still return with `failover_ready=false`; owner death during that window can fail recreate with `ref not found`.
- Remaining fan-out failures increment `aerolvm_secret_fanout_failures_total` and retry through the durable outbox with bounded backoff.

Prefer waiting on `failover_ready` (or your monitoring of that signal) before treating an HA sandbox as failover-safe.

## Boot canary (awskms)

When `SB_SECRET_PROVIDER=awskms`, daemon start runs a one-shot wrap/unwrap canary against the configured CMK. That round-trip is **not** on the sandbox create path.

- Success: log `secret provider boot canary ok`; expvar `aerolvm_secret_provider_canary_ok=1`.
- Failure: warn and continue unless `SB_SECRET_PROVIDER_STRICT_BOOT=true`. Enterprise mode requires strict boot for `awskms`.

## Operational checklist

1. Set `SB_ENTERPRISE_MODE=true`, use a random `SB_PAT_TOKEN` of at least 32 bytes, and keep privileged containers and resource-limit bypasses disabled.
2. Configure `SB_CLUSTER_TLS_DIR`, secure gossip, and secure credential storage on every cluster node; do not expose the internal listener publicly.
3. Keep recipient fan-out enabled with at least two backups and a non-zero `SB_SECRET_FANOUT_MIN_ACK_WAIT`.
4. Enable sealed env and toolbox-token storage, and retain non-zero audit and deletion-tomb history.
5. Share `SB_CREDENTIAL_ENCRYPTION_KEY` (or the key file) across nodes when using `local`.
6. For membership-resilient HA, set `SB_SECRET_PROVIDER=awskms`, enable strict boot, configure the CMK, and grant every worker only the required KMS permissions.
7. Do not set `SB_SECRET_PROVIDER=vault` until the Vault backend ships — boot will refuse.
8. Treat `failover_ready` as the readiness signal for the configured replication level; enterprise create guarantees one backup ACK, not completion of every async replica.
