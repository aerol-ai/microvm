---
title: Cluster Secrets Providers
description: Choose local or AWS KMS for sealing cluster credentials, understand the D5 recipient-set limitation, and configure fan-out for HA failover.
---

Registry passwords and mount credentials for `failover.policy=recreate` sandboxes are sealed outside the Raft log and distributed to recipient peers asynchronously. This page covers which secret provider to pick, what still requires fan-out, and the known limitation when cluster membership drifts after create.

For the broader failover model see [Durability & Failover](/durability). For cluster env vars see [Cluster Glossary](/cluster-glossary).

## Two walls, two fixes

Cross-node recreate historically failed for two independent reasons:

| Wall | Symptom | Fix |
|---|---|---|
| **WALL 1** — bytes never left the sealing node | `cluster secret ref ... not found` on the new owner | Async peer fan-out of the sealed `cluster_secrets` row (`SB_SECRET_RECIPIENT_FANOUT_ENABLED`, default on) |
| **WALL 2** — envelope recipient binding | `recipient ... is not allowed to open` even with the bytes | Local provider: seal to owner + N backup candidates. **Or** use AWS KMS so IAM, not the recipient list, authorizes unwrap |

A shared credential encryption key (`SB_CREDENTIAL_ENCRYPTION_KEY`) is necessary for the local provider but **not sufficient** for HA. Fan-out (or KMS + fan-out) is also required.

## Provider choice

`SB_SECRET_PROVIDER` defaults to `local`. A deployment that never sets it never contacts AWS, Vault, or any external service.

| Value | Key custody | Who can Open | When to use |
|---|---|---|---|
| `local` (default) | Node-local AES key file / `SB_CREDENTIAL_ENCRYPTION_KEY` | Only nodes in the seal recipient set | Default clusters; no external dependency |
| `awskms` | AWS KMS wraps the per-secret data key into the existing envelope `WrappedKey` field (no format change) | Any node with IAM Decrypt **that also received the sealed row** | HA that must survive membership change after create (D5 escape hatch) |
| `vault` | Planned — same `Provider` interface | — | Not implemented; setting this value fails boot with a clear error (no silent fallback to local) |

Both providers still store ciphertext in the local `cluster_secrets` SQLite table. KMS wraps *keys*; it does not store your payload. Fan-out machinery is shared.

### Environment variables

| Variable | Default | Description |
|---|---|---|
| `SB_SECRET_PROVIDER` | `local` | `local` \| `awskms` \| `vault` |
| `SB_SECRET_AWS_KMS_KEY_ID` | (empty) | Required when provider is `awskms` — CMK id, ARN, or alias |
| `SB_SECRET_PROVIDER_STRICT_BOOT` | `false` | When `true`, daemon start fails if the awskms wrap/unwrap canary fails. Default is fail-open with a warning |
| `SB_SECRET_RECIPIENT_FANOUT_ENABLED` | `true` | Recipient-set sealing + async peer fan-out for recreate sandboxes |
| `SB_SECRET_RECIPIENT_BACKUP_COUNT` | `2` | Non-owner candidates in the seal set (owner + this many) |
| `SB_CREDENTIAL_ENCRYPTION_KEY` | (empty) | Shared AES key for the local provider (cluster mode requires this or a key file) |
| `SB_SECRET_ENV_SEAL_ENABLED` | `false` | Sealed sandbox env rows (T7). Format-change flag — enable only after every node can merge env from the provider bag |

## Known limitation (D5): frozen recipient sets

Recipient sets are computed at create (from the placement candidate list) and **do not update** when membership changes later.

With the **local** provider:

1. Create an HA sandbox sealed to `{node-a, node-b, node-c}`.
2. Add `node-d`, drain or lose the original recipients.
3. Failover may pick `node-d`, which has neither the bytes nor recipient permission.
4. Open fails with a distinct `recipient denied` error (not a generic decrypt failure). The owner watcher must not reassign-churn the fleet on that class of error.

**Documented, not fixed** for the local provider. Operators who need HA that survives membership churn should set `SB_SECRET_PROVIDER=awskms`. KMS removes WALL 2 (recipient binding) for nodes whose IAM can Decrypt — but those nodes still need the sealed row via fan-out (WALL 1).

## Async fan-out window and `failover_ready`

Fan-out is asynchronous. A successful create (`201`) does **not** mean every backup already holds the secret.

- `failover_ready` starts false and becomes true only when the owner plus at least one backup hold the sealed row.
- Until then, owner death can still fail recreate with `ref not found`.
- Fan-out failures increment `aerolvm_secret_fanout_failures_total` and retry with bounded backoff; they never fail the create.

Prefer waiting on `failover_ready` (or your monitoring of that signal) before treating an HA sandbox as failover-safe.

## Boot canary (awskms)

When `SB_SECRET_PROVIDER=awskms`, daemon start runs a one-shot wrap/unwrap canary against the configured CMK. That round-trip is **not** on the sandbox create path.

- Success: log `secret provider boot canary ok`; expvar `aerolvm_secret_provider_canary_ok=1`.
- Failure: warn and continue unless `SB_SECRET_PROVIDER_STRICT_BOOT=true`.

## Operational checklist

1. Keep `SB_SECRET_RECIPIENT_FANOUT_ENABLED=true` in production (default).
2. Share `SB_CREDENTIAL_ENCRYPTION_KEY` (or the key file) across nodes when using `local`.
3. For membership-resilient HA, set `SB_SECRET_PROVIDER=awskms` and `SB_SECRET_AWS_KMS_KEY_ID`, and grant every worker `kms:Encrypt` / `kms:Decrypt` on that CMK.
4. Do not set `SB_SECRET_PROVIDER=vault` until the Vault backend ships — boot will refuse.
5. Treat `failover_ready` as the HA readiness signal, not the create response alone.
