---
import { isActiveOrParentPath } from '../utils/navigation'

const { sublist = [], nested = false } = Astro.props
---

<ul class:list={{ 'top-level': !nested }}>
  {
    sublist.map(entry => (
      <li>
        {
          entry.type === 'link' ? (
            <a
              href={entry.href}
              aria-current={isActiveOrParentPath(entry.href, Astro.url.pathname)
                ? 'page'
                : undefined}
              target={entry.external ? '_blank' : undefined}
              class:list={{ active: isActiveOrParentPath(entry.href, Astro.url.pathname) }}
            >
              <span>{entry.label}</span>
            </a>
          ) : entry.type === 'group' ? (
            <div>
              <div class="group-label">
                <span class="large">{entry.label}</span>
              </div>
              <Astro.self sublist={entry.entries} nested />
            </div>
          ) : null
        }
      </li>
    ))
  }
</ul>

<style>
  ul {
    list-style: none;
    padding: 0;
  }

  li {
    overflow-wrap: anywhere;
  }

  .large {
    font-family: var(--font-mono);
    font-size: 0.8125rem;
    font-weight: 700;
    line-height: 1;
    letter-spacing: -0.12px;
    text-transform: uppercase;
    color: var(--primary-text-color);
  }

  .group-label {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 24px 0 8px 0;
  }

  li > a {
    display: flex;
    align-items: center;
    font-family: var(--font-mono);
    font-size: 0.875rem;
    font-weight: 400;
    letter-spacing: -0.04rem;
    text-decoration: none;
    color: var(--secondary-text-color);
    padding: 4px 0;
    transition: color 0.2s ease;
  }

  li > a:hover {
    color: var(--primary-text-color);
  }

  li > a.active,
  li > a[aria-current='page'] {
    color: var(--hover-color);
  }
</style>